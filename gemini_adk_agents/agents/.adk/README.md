# ADK Agent Configuration

This folder configures the agent for use with Google ADK CLI.

## Running the Agent

```bash
# From the gemini_adk_agents directory
adk run research_agent
adk run analyst_agent
```

## Configuration

The `.adk/` folder in each agent directory contains ADK-specific configuration.
